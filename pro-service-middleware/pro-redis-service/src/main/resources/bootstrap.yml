spring:
  application:
    name: redis-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,redis-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware
      discovery:
        enabled: false
      override-none: true
      uri: http://${config-service}:8002