server:
  port: 8086
mybatis:
  type-aliases-package: com.wzy.pojo
  mapper-locations: classpath:mapper/*.xml
management:
  server:
    port: 15003
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  hystrix:
    enabled: true
