eureka:
  instance:
    status-page-url: http://localhost:${server.port}/swagger-ui.html
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: gateway-service
  cloud:
    config:
      profile: dev
      name: gateway-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware
      discovery:
        enabled: true
        service-id: config-service
      override-none: true