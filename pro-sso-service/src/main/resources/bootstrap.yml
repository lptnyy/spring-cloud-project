spring:
  application:
    name: sso-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,sso-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware,mysql-usersqldata
      discovery:
        enabled: false
      override-none: true
      uri: http://localhost:13001
server:
  servlet:
    session:
      cookie:
        name: ssocookie