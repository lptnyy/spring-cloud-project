spring:
  application:
    name: sso-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,sso-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware
      discovery:
        enabled: false
      override-none: true
      uri: http://${config-service}:8002

server:
  servlet:
    session:
      cookie:
        name: sso_cookie

