spring:
  application:
    name: product-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,product-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware,mysql-sqldata
      discovery:
        enabled: false
      override-none: true
      uri: http://localhost:13001