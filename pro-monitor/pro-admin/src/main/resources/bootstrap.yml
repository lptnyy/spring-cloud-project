spring:
  application:
    name: admin-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,admin-service
      discovery:
        enabled: false
      override-none: true
      uri: http://${CONFIG-SERVICE}:8002


