spring:
  application:
    name: user-service-restfull
  cloud:
    config:
      profile: ${spring.profiles.active}
      name: eureka-middleware,user-service-restfull,rabbitmq-middleware,zipkin-middleware,admin-middleware,oauth2-res
      discovery:
        enabled: false
      override-none: true
      uri: http://${ZUUL-SERVICE}:8101/config
