spring:
  application:
    name: system-service-app
  cloud:
    config:
      profile: ${spring.profiles.active}
      name: eureka-middleware,system-service-app,rabbitmq-middleware,zipkin-middleware,admin-middleware,oauth2-res
      discovery:
        enabled: false
      override-none: true
      uri: http://${ZUUL-SERVICE}:8101/config
  main:
    allow-bean-definition-overriding: true
