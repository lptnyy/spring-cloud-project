spring:
  application:
    name: role-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,role-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware,mysql-rolesqldata
      discovery:
        enabled: false
      override-none: true
      uri: http://localhost:13001