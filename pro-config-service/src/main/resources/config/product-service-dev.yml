server:
  port: 8087
mybatis:
  type-aliases-package: com.wzy.pojo
  mapper-locations: classpath:mapper/*.xml
management:
  server:
    port: 15004
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  hystrix:
    enabled: true
