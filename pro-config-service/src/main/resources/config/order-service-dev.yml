server:
  port: 8007
mybatis:
  type-aliases-package: com.wzy.pojo
  mapper-locations: classpath:mapper/*.xml
management:
  server:
    port: 10005
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  hystrix:
    enabled: true
