spring:
  application:
    name: zuul-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,zuul-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware,oauth2-res
      discovery:
        enabled: false
      override-none: true
      uri: http://${config-service}:8002