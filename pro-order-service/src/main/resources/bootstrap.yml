spring:
  application:
    name: order-service
  cloud:
    config:
      profile: dev
      name: eureka-middleware,order-service,redis-nosql,rabbitmq-middleware,zipkin-middleware,admin-middleware,mysql-ordersqldata
      discovery:
        enabled: false
      override-none: true
      uri: http://localhost:13001