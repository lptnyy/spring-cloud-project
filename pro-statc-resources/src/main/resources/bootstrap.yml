spring:
  application:
    name: static-resources
  cloud:
    config:
      profile: dev
      name: eureka-middleware,static-resources,rabbitmq-middleware,zipkin-middleware
      discovery:
        enabled: false
      override-none: true
      uri: http://localhost:13001
  main:
    allow-bean-definition-overriding: true